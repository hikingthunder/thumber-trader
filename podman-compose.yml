version: '3.8'

services:
  app:
    build: .
    container_name: thumber-trader
    ports:
      - "8080:8080"
    volumes:
      - .:/app:Z
      - data_volume:/app/data:Z
    env_file:
      - .env
    restart: unless-stopped

volumes:
  data_volume:
